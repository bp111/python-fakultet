[project]
name = "mutt_journal"
version = "1.0.0"
description = "Digital journal for dogs to read and have some fun in their silly life."
dependencies = [ 
    "flask",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi" 